IMPORTANT:

La base de données est hébergée sur le Cloud via Supabase, on a utilisé SQLAlchemy (voir "models.py") pour la créer et l'utiliser.
On l'avait déjà rempli avec des données (users et favoris).